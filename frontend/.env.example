# Optional. Leave blank during local development — Vite's dev proxy
# (vite.config.js) forwards /api and /socket.io to http://localhost:5000
# automatically. Set this if you deploy the frontend and backend to
# different hosts (e.g. VITE_API_URL=https://api.your-domain.com).
VITE_API_URL=
